framework: mocha
on_start:
  command: browserstack tunnel localhost:7357
  wait_for_text: Tunnel is running

launchers:

  bs_chrome:
    command: browserstack launch --attach chrome localhost:7357
    protocol: browser

launch_in_ci: [bs_chrome]

launch_in_dev: [bs_chrome]
