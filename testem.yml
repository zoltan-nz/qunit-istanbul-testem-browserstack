framework: mocha
on_start:
  command: browserstack -t 360 tunnel localhost:<port>
  wait_for_text: Tunnel is running

launchers:

  bs_chrome:
    command: browserstack -t 360 launch --attach chrome <url>
    protocol: browser

launch_in_ci: [bs_chrome]

launch_in_dev: [bs_chrome]
