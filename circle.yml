machine:
  timezone:
    Pacific/Auckland

dependencies:
  pre:
    - mv ./.browserstack ~/
    - sh ~/.browserstack/runthis.sh

  post:
    - npm install browserstack-cli -g
    - npm install testem -g

test:
  override:
    - testem ci
